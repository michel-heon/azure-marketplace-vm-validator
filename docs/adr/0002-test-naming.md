# ADR 0002 — Test naming

## EN
The naming convention is `test_<id>_<area>.sh`.
This keeps ordering stable and intent explicit.

## FR
La convention de nommage est `test_<id>_<zone>.sh`.
Elle garantit un ordre stable et une intention explicite.
